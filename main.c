#include "monty.h"

/**
 *
 *
 *
 */
int main(int argc, char **argv)
{
	if (argc != 2)
	{
		printf("USAGE: monty file");
		exit(EXIT_FAILURE);
	}

	//if (FILE CANT OPEN )
	//{
	//	printf("Error: Can't open file %s\n", <FILENAME>);
	//	exit(EXIT_FAILURE);
	//}

	//if (FILE HAS INVALID INSTRUCTIONS)
	//{
	//	printf("L%u<line_number>: unknown instruction %s<opcode>\n",
	//	       line_num, opcode );
	//	exit(EXIT_FAILURE);
	//}

	//if (CANT MALLOC MORE SPACE)
	//{
	//	printf("Error: malloc failed");
	//	exit(EXIT_FAILURE);
	//}
}
